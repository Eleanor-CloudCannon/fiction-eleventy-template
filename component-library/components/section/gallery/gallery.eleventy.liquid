<section class="gallery">
  <div class="container">
    <div class="row">
      <div class="gallery-description text-center">
        <p>{{ content | safeHTML }}</p>
      </div>
      {% for items in gallery_images %}
        {% bookshop "simple/gallery-image" %}
      {% endfor %}
    </div>
    <!-- /.End row -->
  </div>
</section>